[package]
name = "lau-circuit"
version = "0.1.0"
edition = "2021"
description = "Deadband circuit system — self-contained automations that run within tolerance bands"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
